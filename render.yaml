services:
  - type: web
    name: glosas-clinica-porvenir
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python dashboard/app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
